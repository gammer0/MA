# 开发日志

## 2026-04-30

### 项目初始化 (commit: f0f9eaa)
- 创建项目骨架目录结构
- 切换到 `feature/identity-service` 分支
- 创建 .gitignore, .env.example, readme.md
- identity-service: config.py, requirements.txt, Dockerfile

### crypto.py (commit: 55b5572)
- Ed25519 密钥对生成: `generate_ed25519_keypair()`
- 签名 payload 规范化构建: `build_session_signature_payload()`
- 签名: `sign_payload()`
- 验签: `verify_signature()`

### models.py (commit: 424de5e)
- Agent 数据模型: AgentRecord, CertStatus, AgentType
- Tool 数据模型: ToolRecord, ToolStatus
- 请求模型: RegisterAgentRequest, VerifySignatureRequest, RegisterToolRequest
- 响应模型: RegisterAgentResponse, AgentResponse, ToolResponse 等

### cert_store.py (commit: 07f7cd7)
- Agent 证书 PG 存储: store, get, list, update_status, update_key
- 轻量查询: get_agent_public_key (用于验签)

### tool_store.py + cache.py (commit: 10d076e)
- MCP 工具 PG 存储: store, get, list, get_by_owner_and_name, update_status
- Redis 缓存: cache_agent_public_key, get_cached_public_key, invalidate_agent_cache

### handlers.py (commit: 0303ca0)
- Agent 管理: register, get, list, revoke, renew
- 签名验证: verify/signature (供权限网关调用)
- 公钥获取: agents/{id}/public-key
- MCP 工具: register, get, list, revoke

### main.py + middleware.py (commit: 99150c3)
- FastAPI 入口 + lifespan (PG/Redis 连接管理)
- AdminAPIKeyMiddleware: 管理接口认证
- ServiceAPIKeyMiddleware: 服务间接口认证

### 数据库 + 脚本 (commit: 78aa0c7)
- init_db.sql: 全服务数据库 DDL (含权限网关 + 审计模块表)
- register_agent.py: 管理员注册 Agent 命令行脚本

