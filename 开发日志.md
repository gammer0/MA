# 开发日志

## 2026-04-30

### 项目初始化 (commit: f0f9eaa)
- 创建项目骨架目录结构
- 切换到 `feature/identity-service` 分支
- 创建 .gitignore, .env.example, readme.md
- identity-service: config.py, requirements.txt, Dockerfile

### crypto.py (commit: 55b5572)
- Ed25519 密钥对生成: `generate_ed25519_keypair()`
- 签名 payload 规范化构建: `build_session_signature_payload()`
- 签名: `sign_payload()`
- 验签: `verify_signature()`

### models.py (commit: 424de5e)
- Agent 数据模型: AgentRecord, CertStatus, AgentType
- Tool 数据模型: ToolRecord, ToolStatus
- 请求模型: RegisterAgentRequest, VerifySignatureRequest, RegisterToolRequest
- 响应模型: RegisterAgentResponse, AgentResponse, ToolResponse 等

### cert_store.py (commit: 07f7cd7)
- Agent 证书 PG 存储: store, get, list, update_status, update_key
- 轻量查询: get_agent_public_key (用于验签)

### tool_store.py + cache.py (commit: 10d076e)
- MCP 工具 PG 存储: store, get, list, get_by_owner_and_name, update_status
- Redis 缓存: cache_agent_public_key, get_cached_public_key, invalidate_agent_cache

### handlers.py (commit: 0303ca0)
- Agent 管理: register, get, list, revoke, renew
- 签名验证: verify/signature (供权限网关调用)
- 公钥获取: agents/{id}/public-key
- MCP 工具: register, get, list, revoke

### main.py + middleware.py (commit: 99150c3)
- FastAPI 入口 + lifespan (PG/Redis 连接管理)
- AdminAPIKeyMiddleware: 管理接口认证
- ServiceAPIKeyMiddleware: 服务间接口认证

### 数据库 + 脚本 (commit: 78aa0c7)
- init_db.sql: 全服务数据库 DDL (含权限网关 + 审计模块表)
- register_agent.py: 管理员注册 Agent 命令行脚本

---

### 权限网关 - feature/permission-gateway 分支

#### 骨架 + models (commit: 2d5cbeb)
- config.py, requirements.txt, Dockerfile
- models.py: 6 个核心模型 (TokenEntry, StandardToken, TaskPermissionEntry, TokenView, PermissionRequest, Session) + 完整请求/响应模型

#### 存储层 (commit: 7c49c71)
- token_manager.py: StandardToken CRUD + 条目级管理
- task_permissions.py: 任务临时权限 CRUD
- permission_requests.py: 权限申请/审批 CRUD

#### 核心模块 (commit: 3e43fbd)
- view_builder.py: 单Agent视图(并集) + 多Agent视图(交集+deny优先) + 权限判定 evaluate_view
- session_manager.py: 会话CRUD + Redis视图缓存 + 任务级清理
- identity_client.py: 身份注册服务 HTTP 客户端 (验签)
- audit_client.py: 审计模块异步 fire-and-forget 客户端

#### API 入口 (commit: e948039)
- handlers.py: 完整 API 路由
  - Standard Token 管理 (7 个接口)
  - 任务临时权限 (4 个接口)
  - 权限申请审批 (4 个接口)
  - 运行时调用拦截 (POST /gateway/call, GET /sessions/{id}/view)
- middleware.py: AdminAPIKeyMiddleware + ServiceAPIKeyMiddleware
- main.py: FastAPI 入口

---

### 审计模块 - feature/audit-service 分支

#### 骨架 + models (commit: 2d49f23)
- config.py, requirements.txt, Dockerfile
- models.py: 5 种审计记录模型 (SignatureRecord, SessionLog, PermissionDecision, TaskLifecycleEvent, PermissionRequestLog) + TraceNode 树形结构 + 查询响应模型

#### 存储层 + 调用链 (commit: 4575c9f)
- log_store.py: 5 种日志持久化函数
- trace_builder.py: 调用链树形建模 (build_task_trace) + Agent历史查询 + 任务会话查询 + 权限申请历史

#### API 入口 (commit: eb7f461)
- handlers.py: 5 个写入接口 + 4 个查询接口
  - 写入: signature-records, session-logs, permission-decisions, task-events, permission-request-logs
  - 查询: tasks/{id}/trace, tasks/{id}/sessions, agents/{id}/history, tasks/{id}/permission-requests
- middleware.py: ServiceAPIKeyMiddleware(写入) + AdminAPIKeyMiddleware(查询)
- main.py: FastAPI 入口

---

### 执行层 - feature/execution-layer 分支

#### 骨架 + SDK (commit: 91b3db9)
- config.py, requirements.txt, Dockerfile
- agent_sdk/signing_utils.py: Ed25519 签名工具
- agent_sdk/secure_agent_client.py: SecureAgentClient 基类 (call_agent/call_mcp_tool/request_permission/wait_for_approval/finalize_task)
- gateway_client.py: 权限网关 HTTP 客户端

#### MCP 工具集 (commit: d390296)
- mcp_tools/base.py: BaseTool 抽象基类
- mcp_tools/searcher_tools.py: WebSearchTool + PageFetchTool (mock)
- mcp_tools/analyzer_tools.py: CalcTool + ChartGenTool (mock)
- mcp_tools/public_tools.py: FileReadTool + FileWriteTool (mock)
- 共计 6 个工具，覆盖 public / searcher / analyzer 三种属主

#### Agent 实现 (commit: cb3e9e7)
- worker_agents/base_worker.py: BaseWorker 基类
- worker_agents/searcher_agent.py: 搜索器 (调 web_search + page_fetch)
- worker_agents/analyzer_agent.py: 分析器 (调 calc + chart_gen + searcher A2A)
- orchestrator.py: 编排器 (调度 searcher + analyzer + file_write + finalize)

#### Web UI + 入口 (commit: 66c1894)
- main.py: FastAPI 入口 + 3 Agent 实例初始化 (演示用硬编码凭证)
- web_ui.py: Web UI 路由 (POST /tasks/execute, WebSocket /ws)
- static/index.html: 演示控制台 (自然语言输入, 调用链展示, 审计日志, 安全事件)